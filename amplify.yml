version: 1
frontend:
  phases:
    build:
      commands:
        - npm cache clear
        - npm install
        - npm run start
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths: []
