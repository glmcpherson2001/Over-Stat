version: 1
frontend:
  phases:
    build:
      commands:
        - npm install
    
    postBuild:
      - node server.js
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths: []
