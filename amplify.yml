version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run start 
frontend:
  phases:
    preBuild: 
      commands:
    build:
    preBuild:
  artifacts:
    baseDirectory: views
    files:
      - "**/*"
  cache:
    paths: 
      - node_modules/**/*
