version: 1
frontend:
  phases:
    preBuld:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /views
    files:
      - "**/*"
  cache:
    paths: []
