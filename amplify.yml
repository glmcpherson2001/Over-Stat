version: 1
backend:
  phases:
    preBuild: 
      commands:
    build: 
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run start
  artifacts:
    baseDirectory: views
    files:
      - "**/*"
  cache:
    paths: 
      - node_modules/**/*
