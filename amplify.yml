version: 1
frontend:
  phases:
    build:
      commands:
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
backend:
  phases:
    build:
      commands:
        - npm install
    postBuild:
      commands:
        - node server.js
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
  

