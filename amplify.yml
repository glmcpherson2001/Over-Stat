version: 1
frontend:
  phases:
    build:
      commands:
        - npm install
    
    postBuild:
      commands:
        - node server.js
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths: []
