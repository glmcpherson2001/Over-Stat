version: 1
frontend:
  phases:
    build:
      commands:
        - npm install --no-audit
        - npm run build
  artifacts:
    baseDirectory: /build
    files:
      - "**/*"
  cache:
    paths: []
