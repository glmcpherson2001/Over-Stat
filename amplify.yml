version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: views
    files:
      - "**/*"
  cache:
    paths: 
      - node_modules/**/*
