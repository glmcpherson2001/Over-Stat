version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm i ejs express
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: views
    files:
      - "**/*"
  cache:
    paths: 
      - node_modules/**/*
